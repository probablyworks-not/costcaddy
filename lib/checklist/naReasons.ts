// Fixed N/A reasons — not wired in the auditor .dc.html at all (EXECUTION.md R3); this
// list is copied from the product spec (story A-8), the one place the design-only
// build rule doesn't apply.
export const NA_REASONS = [
  'Not applicable at this outlet',
  'Record / register not maintained',
  'Document not available during audit',
  'Area or asset not accessible',
  'Responsible staff unavailable',
] as const;
